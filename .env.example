# Cloudflare Web Analytics token (optional, build-time).
# Set in GitHub repo Settings → Secrets and variables → Actions → Variables.
# When unset, no beacon script is emitted — fully cookieless build.
PUBLIC_CF_BEACON_TOKEN=
