# build output
dist/
.astro/

# dependencies
node_modules/

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production
.env.local
.env.*.local

# editor / OS
.DS_Store
.idea/
.vscode/*
!.vscode/extensions.json
!.vscode/launch.json
!.vscode/settings.json
*.swp
Thumbs.db

# Pagefind temp
src/pagefind/

# Cloudflare Wrangler (if we ever use it)
.wrangler/

# Belt-and-braces — the public site repo must NEVER carry private artifacts.
#
# learn-vault/  is the LLM-maintained Obsidian brain, kept private (one dir up).
# raw/ and ingest/  no longer exist locally — vendor KB scraping/embedding is
#                   handled by the external vendor-rag MCP. Kept here as a
#                   safety net in case anyone ever copies the RAG's raw output
#                   into this tree.
# .claude/      contains launch.json + agent config (one dir up).
# CLAUDE.md and WIKI.md  are project / vault schema docs, never published.
learn-vault/
raw/
ingest/
.claude/
.agents/
.codex/
CLAUDE.md
WIKI.md
DNSFILTER.md
docs/
